{
   "name": "bulma-start-muncie",
   "version": "0.0.1",
   "description": "Kevin Muncie's start package for Bulma",
   "keywords": [
      "sass",
      "bulma",
      "css",
      "start",
      "scaffold"
   ],
   "author": "Kevin Muncie (http://kmuncie.com)",
   "license": "MIT",
   "devDependencies": {
      "autoprefixer": "^7.1.5",
      "babel-cli": "^6.26.0",
      "babel-preset-env": "^1.6.0",
      "babel-preset-es2015-ie": "^6.7.0",
      "bulma": "^0.6.0",
      "cpx": "^1.5.0",
      "node-sass": "^4.5.3",
      "npm-run-all": "^4.1.3",
      "postcss-cli": "^4.1.1"
   },
   "scripts": {
      "clean": "rm -rf dist",
      "css-deploy": "npm run css-build && npm run css-postcss",
      "css-build": "node-sass -r --output-style compressed src/scss/collector.scss -o dist/css/",
      "css-postcss": "postcss --use autoprefixer --output dist/css/style.css dist/css/style.css",
      "css-watch": "npm run css-build && npm run css-build -- -wr",
      "js-build": "babel src/js --out-dir dist/js",
      "js-watch": "npm run js-build -- --watch",
      "html-build": "cpx src/*.html dist -vu",
      "html-watch": "npm run html-build -- -w",
      "setup-dist": "mkdir dist/css dist/js -p && touch dist/css/style.css",
      "develop": "npm run setup-dist && npm-run-all --parallel html-build css-watch js-watch css-postcss",
      "generate": "npm run clean && npm run setup-dist && npm run html-build && npm run css-deploy && npm run js-build"
   }
}
